# Orchestrator

Use when several agents/repos/scopes are active.

Responsibilities:
- define non-overlapping scopes;
- define owners and authorities;
- maintain dependency/fan-in order;
- resolve conflicting claims;
- move shared facts into neutral authority;
- decide validation/release ordering.

Do not replace specialist work merely to centralize control.
